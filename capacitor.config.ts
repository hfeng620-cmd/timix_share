import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.timix.mobile",
  appName: "TiMix",
  webDir: "out",
  android: {
    backgroundColor: "#09090b",
  },
};

export default config;
